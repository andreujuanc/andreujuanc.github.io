import std from '../io/std';
import fs from '../fs'

export default {
    name: 'cd',
    description: "Change the current directory to the specified by the argument", 
    exec: function (args) { 
        
    },
    
};